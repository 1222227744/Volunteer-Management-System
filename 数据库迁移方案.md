# 数据库迁移方案

## 1. 目标

本方案用于把当前仓库中的数据库结构调整，从“启动时临时修补”逐步整理为“有版本号、可追踪、可复现”的轻量迁移体系。

当前方案仍保持课程项目可运行优先，不一次性引入 `Flyway/Liquibase`，而是先在现有 Spring Boot 单体中建立以下约束：

- 初始化建库仍可使用 `backend/src/main/resources/sql/init.sql`
- 增量结构变更统一记录在 `backend/src/main/resources/sql/migrations/`
- 应用启动时由 `SchemaMigrationInitializer` 读取版本脚本并登记历史
- 已经满足目标结构的旧库按 `BASELINED` 记入历史，不重复执行破坏性 SQL

## 2. 当前组成

### 2.1 初始结构

- `backend/src/main/resources/sql/init.sql`

作用：

- 用于新环境一次性初始化完整表结构、约束和索引
- 适合课程演示、首次部署和本地快速建库

### 2.2 增量迁移脚本

- `backend/src/main/resources/sql/migrations/V2_001__*.sql`
- `backend/src/main/resources/sql/migrations/V2_010__*.sql`

作用：

- 记录 `v2` 阶段已经落地的结构修正与数据迁移
- 为已有数据库提供逐步追平路径

### 2.3 迁移历史表

- `schema_migrations`

记录字段包括：

- `version`：迁移版本号
- `description`：迁移说明
- `script_path`：脚本路径
- `checksum`：脚本校验和
- `apply_mode`：`EXECUTED` 或 `BASELINED`
- `note`：兼容判定说明
- `installed_at`：登记时间

## 3. 当前迁移脚本清单

| 版本 | 作用 |
| --- | --- |
| `V2_001` | 为 `activity_registrations` 补充 `check_out_at` 字段 |
| `V2_002` | 扩展旧版 MySQL `ENUM` 报名状态定义 |
| `V2_003` | 将历史 `REGISTERED` 数据回填为 `APPROVED` |
| `V2_004` | 将旧广播通知拆分为用户级通知记录 |
| `V2_005` | 为 `service_records(activity_id, user_id)` 补充唯一约束 |
| `V2_006` | 为报名表补充活动状态联合索引 |
| `V2_007` | 为通知表补充用户已读联合索引 |
| `V2_008` | 为服务记录表补充用户时间索引 |
| `V2_009` | 为审计日志补充时间索引 |
| `V2_010` | 为审计日志补充动作时间索引 |

## 4. 启动时执行规则

`SchemaMigrationInitializer` 会按固定顺序处理脚本：

1. 创建 `schema_migrations`
2. 逐个检查迁移版本是否已登记
3. 对未登记版本做兼容判定
4. 若当前库缺少目标结构，则执行 SQL 并登记为 `EXECUTED`
5. 若当前库已满足目标结构，则仅登记为 `BASELINED`
6. 若发现重复数据等无法安全自动修复的问题，则输出告警并停止该版本自动迁移

这样做的目的：

- 避免重复执行 `ALTER TABLE`
- 避免旧库上因约束已存在而报错
- 保留迁移历史，方便答辩、验收和后续排查

## 5. 与 `ddl-auto=update` 的关系

当前项目仍保留：

- `spring.jpa.hibernate.ddl-auto=update`

原因：

- 课程项目部署环境更偏向快速运行
- 现阶段仍存在 JPA 映射和初始化 SQL 并行维护的情况

但需要明确：

- 结构治理以 `init.sql` 和 `sql/migrations/` 为准
- `ddl-auto=update` 只作为开发环境兼容手段，不应被视为正式迁移记录
- 后续若项目继续演进，推荐逐步收紧为 `validate`，再切换到标准迁移框架

## 6. 后续变更约定

从本次迭代开始，新增数据库结构调整建议遵循以下流程：

1. 先更新实体或仓库代码
2. 补充对应版本迁移脚本，命名为 `V2_xxx__说明.sql`
3. 如有兼容旧库需求，在 `SchemaMigrationInitializer` 中加入判定逻辑
4. 若新环境也需要该结构，记得同步 `init.sql`
5. 在仓库文档中更新迁移说明或对应模块说明

## 7. 当前仍保留的限制

当前方案仍是轻量治理，而不是完整迁移平台，因此有以下限制：

- 不支持脚本回滚
- 不支持并行环境的严格锁控制
- 不支持复杂数据清洗编排
- 对“存在脏数据才能修复”的场景仍以告警和人工处理为主

这对课程版 `v2` 已经足够，但如果后续继续演进为长期项目，建议引入 `Flyway` 或 `Liquibase`。
