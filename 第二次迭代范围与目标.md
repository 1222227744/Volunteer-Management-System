# 第二次迭代范围与目标

## 1. 基线文档

第二次迭代以以下文档作为统一基线：

- `软件需求规格说明书_2026-v1.1.docx/.pdf`
- `概要设计v1.docx/.pdf`
- `志愿者管理系统_可行性分析报告(V2.0).docx/.pdf`
- 仓库内同步文档：
  - `README.md`
  - `系统设计说明书.md`
  - `需求实现对应矩阵.md`
  - `数据库迁移方案.md`

本文件的作用是把“最新源文档中的目标能力”与“当前仓库实现状态”衔接起来，明确第二次迭代已经完成了什么，还剩哪些应继续推进的工程项。

## 2. 当前版本定位

当前仓库已经从第一版 `MVP` 进入第二次迭代的落地阶段，具备以下特征：

- 核心业务主链已经可运行、可演示、可验收。
- `v2` 重点闭环已经补齐，不再只是规划状态。
- 当前剩余工作主要集中在文档治理、数据库迁移治理和自动化测试覆盖，而不是重新设计业务主流程。

因此，第二次迭代的定位是：

- 不重做架构
- 不追求外部系统集成
- 将课程版系统收口为“文档一致、流程完整、治理可信、可继续演进”的第二版

## 3. 本次迭代总目标

第二次迭代聚焦四个目标：

1. 让最新需求文档、设计文档和仓库内说明文档口径一致。
2. 把活动主流程从“能跑通”提升到“状态、通知、审计都成闭环”。
3. 补齐课程版 `FR-05` 中最关键但曾缺失的“活动评价”能力。
4. 修正会影响验收和演示可信度的治理问题，例如广播通知已读、审计假分页、组织方数据越权可见、迁移方案不可追踪等。

## 4. 本次迭代开发范围与完成情况

### 4.1 核心业务闭环补齐

已完成：

- 活动状态流转规则收紧，拒绝不合理的直接跳转。
- 活动取消时同步处理相关报名状态、发送通知并写入审计日志。
- 服务记录登记收紧为签到签退完成后才能登记，并阻止重复提交。
- 志愿者可基于正式服务记录提交活动评价，组织方和管理员可查看评价结果。

仍待继续增强：

- 志愿者主动取消报名流程
- 异常签到签退更正与留痕
- 更细化的活动报名规则

### 4.2 通知与审计治理收口

已完成：

- 广播通知改为用户级独立通知记录，已读状态不再共享。
- 报名审核结果、活动状态变更、活动取消、反馈处理等关键通知场景已补齐。
- 审计日志查询改为数据库侧真实筛选、真实分页和条件导出。
- 登录、登出、反馈、捐赠、活动评价等关键操作已补充审计。

仍待继续增强：

- 进一步扩大横切审计覆盖面
- 增加更系统的审计回归测试

### 4.3 权限边界与统计口径收口

已完成：

- 管理员查看全局统计。
- 组织方仅查看自己活动范围内的聚合统计。
- 组织方查询志愿者服务记录时必须绑定到自己负责的活动范围。
- 管理端对捐赠、反馈等平台级数据访问继续保留给管理员。

仍待继续增强：

- 对更多后台列表页继续补充更细粒度的数据边界校验

### 4.4 数据模型与迁移治理

已完成：

- 为服务记录补齐活动与用户联合唯一约束。
- 为报名、通知、服务记录、审计等高频表补齐关键索引。
- 将 `v2` 结构修补整理为版本化 SQL 脚本目录。
- 启动期新增 `schema_migrations` 历史表与兼容判定逻辑。
- 演示数据已覆盖活动评价、通知、反馈、审计等新增流程。

仍待继续增强：

- 自动回滚能力
- 更标准化的迁移框架接入
- 更复杂的数据清洗编排

## 5. 本轮已解决的关键问题

以下问题已在第二次迭代中完成收口：

- 广播通知已读状态共享问题
- 活动状态变更缺少报名联动和通知联动
- 审计日志查询、分页和导出的假分页问题
- 组织方可见数据范围过宽的问题
- 服务记录重复登记问题
- 课程版活动评价缺失问题

## 6. 暂不纳入第二次迭代

以下内容属于后续扩展，不作为本轮强制交付目标：

- `Redis/JWT` 登录态升级
- 短信、邮件、真实支付接口接入
- 通用文件上传、对象存储、附件管理平台化
- 独立“支持者/捐赠者”账号角色体系
- 账号锁定、停用、密码错误次数控制等完整安全治理
- 勋章、星级、积分商城等更复杂的激励体系
- MQ、分布式任务调度、复杂缓存架构

## 7. 本轮验收标准

第二次迭代完成后，至少应满足以下验收标准：

- 文档口径一致：`SRS`、概要设计、可行性分析与仓库内文档不再明显冲突。
- 广播通知按用户独立已读，不再出现共享已读问题。
- 活动取消或关键状态变化具备报名处理、通知和审计联动。
- 志愿者可以基于正式服务记录提交基础活动评价。
- 审计日志支持真实筛选、真实分页和条件导出。
- 组织方与管理员的数据访问边界更加清晰。
- 数据库结构和迁移方案可复现，演示数据能覆盖新增流程。

## 8. 当前剩余建议事项

若继续推进 `v2` 收尾，建议优先顺序如下：

1. 完成仓库内文档回填与概要设计同步修订。
2. 补齐高价值自动化测试，覆盖通知、活动状态、评价与审计筛选等关键回归点。
3. 继续把轻量迁移治理演进为更标准的迁移框架。
4. 再考虑附件、异常更正、更多后台治理功能等扩展能力。

这样可以先把“当前版本是否可信”做实，再进入下一轮业务扩展。
