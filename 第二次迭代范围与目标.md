# 第二次迭代范围与目标

## 1. 基线文档

第二次迭代以以下文档作为统一基线：

- `软件需求规格说明书_2026-v1.1.docx/.pdf`
- `概要设计v1.docx/.pdf`
- `志愿者管理系统_可行性分析报告(V2.0).docx/.pdf`
- 仓库内同步文档：
  - `README.md`
  - `系统设计说明书.md`
  - `需求实现对应矩阵.md`

本文件的作用是把“最新源文档中的目标能力”与“当前仓库的第一版实现”衔接起来，明确第二次迭代到底做什么、不做什么。

## 2. 当前版本定位

当前仓库已经完成第一版 `MVP`，具备课程演示所需的核心业务主链：

- 用户注册、登录、权限控制
- 活动发布、报名、审核、签到签退
- 服务记录、积分排行
- 内容投稿、审核、公告、通知
- 捐赠、反馈、统计看板、审计日志

但从最新 `SRS`、概要设计和可行性分析看，当前版本仍存在两类差距：

- 文档已写明、但代码尚未完整落地的功能缺口
- 功能已具备、但流程联动、权限边界、数据模型和治理能力仍待收口的实现缺口

因此，第二次迭代的定位是：

- 不重做架构
- 不追求外部系统集成
- 专注把课程版 `MVP` 收口为“文档一致、流程完整、治理可信”的第二版

## 3. 本次迭代总目标

第二次迭代聚焦四个目标：

1. 让最新需求文档、设计文档和仓库内说明文档口径一致。
2. 把活动主流程从“能跑通”提升到“状态、通知、审计都成闭环”。
3. 补齐课程版 `FR-05` 中最关键但仍缺失的“活动评价”能力。
4. 修正当前实现中会影响验收和演示可信度的治理问题，例如广播通知已读、审计假分页、组织方数据越权可见等。

## 4. 本次迭代开发范围

### 4.1 核心业务闭环补齐

- 活动状态流转规则收紧：
  - 明确允许的状态迁移
  - 拒绝不合理的直接跳转
- 活动取消联动补齐：
  - 取消活动时同步处理相关报名状态
  - 生成对应通知
  - 写入审计日志
- 报名与服务记录规则收紧：
  - 避免重复登记服务记录
  - 明确签到、签退、完成之间的状态约束
  - 为后续异常更正保留留痕位置
- 补齐基础活动评价闭环：
  - 志愿者在具备正式服务记录后可提交活动评价
  - 组织方或管理员可查看评价结果
  - 前端提供基础评价入口和结果展示

### 4.2 通知与审计治理收口

- 修正广播通知已读模型：
  - 广播消息不再共享同一条已读状态
  - 每个用户独立维护自己的已读结果
- 补齐关键通知触发场景：
  - 报名审核结果
  - 活动取消或关键状态变更
  - 反馈处理回执
- 审计日志能力收口：
  - 查询改为数据库侧真实筛选
  - 分页改为真实分页
  - 导出基于查询条件生成结果
  - 补齐关键操作审计覆盖

### 4.3 权限边界与统计口径收口

- 收紧组织方的数据访问范围：
  - 组织方优先只看自己活动相关的数据
  - 对全平台数据的访问保留给管理员
- 明确统计看板口径：
  - 管理员看全局
  - 组织方看自己负责活动的聚合数据
- 梳理反馈、捐赠、服务记录等页面的数据可见边界

### 4.4 数据模型与迁移治理

- 为核心表补齐必要的唯一约束、索引和外键策略
- 为活动、报名、服务记录、通知、审计等高频表优化查询结构
- 把临时性的运行时修补迁移，逐步整理为可追踪的数据库迁移方案
- 同步更新演示数据，覆盖第二次迭代新增流程

## 5. 当前已知问题，需要计入本轮

以下问题已在走查中确认，应直接纳入第二次迭代：

- 广播通知已读状态为共享记录，任一用户已读后会影响其他用户。
- 活动状态更新目前只改状态并写审计，没有做报名联动和通知联动。
- 审计日志查询、分页和导出目前依赖最近 2000 条内存过滤，不是真正的数据库分页。
- 组织方当前可以看到过宽的平台级统计、捐赠或反馈数据。
- 服务记录前后端规则未完全一致，仍需收紧状态校验与重复登记防护。
- 部分关键操作的审计覆盖仍不完整，例如登录、登出、提交反馈、提交捐赠等。

## 6. 暂不纳入第二次迭代

以下内容属于后续扩展，不作为本轮强制交付目标：

- `Redis/JWT` 登录态升级
- 短信、邮件、真实支付接口接入
- 通用文件上传、对象存储、附件管理平台化
- 独立“支持者/捐赠者”账号角色体系
- 账号锁定、停用、密码错误次数控制等完整安全治理
- 勋章、星级、积分商城等更复杂的激励体系
- MQ、分布式任务调度、复杂缓存架构

## 7. 本轮验收标准

第二次迭代完成后，至少应满足以下验收标准：

- 文档口径一致：`SRS`、概要设计、可行性分析与仓库内文档不再明显冲突。
- 广播通知按用户独立已读，不再出现共享已读问题。
- 活动取消或关键状态变化具备报名处理、通知和审计联动。
- 志愿者可以基于正式服务记录提交基础活动评价。
- 审计日志支持真实筛选、真实分页和条件导出。
- 组织方与管理员的数据访问边界更加清晰。
- 数据库结构和迁移方案可复现，演示数据能覆盖新增流程。

## 8. 推荐开发顺序

建议按以下顺序推进第二次迭代：

1. 先统一文档口径和迭代边界。
2. 再调整数据库结构与迁移方案。
3. 然后补后端流程联动、权限和审计。
4. 最后补前端页面与交互，并刷新演示数据。

这样可以先把“定义什么是完成”说清楚，再进入实际开发，避免中途边界漂移。
