# 志愿者服务管理系统最终技术栈与系统架构方案

## 1. 文档目的

本文件用于统一本项目的技术选型和系统架构口径，作为后续概要设计、详细设计、开发分工、测试和答辩说明的依据。

本文件已经合并并替代此前分散的技术选型说明，最终结论以本文件为准。

## 2. 选型依据

本项目的软件需求规格说明书与当前代码现状，决定了技术选型必须满足以下前提：

- 系统采用 `B/S` 架构，支持 PC 和移动浏览器访问。
- 前后端分离，接口采用 `RESTful + JSON`。
- 首版优先 `模块化单体`，不采用微服务拆分。
- 需要支持用户权限、活动管理、报名审核、签到签退、服务记录、内容审核、通知、反馈、捐赠、统计和审计日志。
- 需要满足课程项目的可交付性、可演示性和可维护性。
- 需要预留短信、邮件、支付、统一认证等外部接口扩展点。

结合当前仓库，现有实现已经具备以下基础：

- 前端：`Vue 3 + Vite`
- 后端：`Java 17 + Spring Boot 3.2 + Spring Data JPA`
- 数据库：`MySQL 8`
- 已实现模块：认证、用户、活动、报名、服务记录、内容、公告、通知、反馈、捐赠、统计、审计

因此，最终方案应以 `连续演进` 为原则，而不是整体推翻重做。
本文件中的技术栈需区分“当前已落地”和“后续建议引入”两类口径。

## 3. 最终技术栈决策

### 3.1 前端技术栈

- 当前已落地：
  - 框架：`Vue 3`
  - 语言：`JavaScript`
  - 构建工具：`Vite`
  - 路由：`Vue Router`
  - 样式方案：`CSS`
- 后续建议引入：
  - `TypeScript`
  - `Pinia`
  - `Axios`
  - `Element Plus`
  - `ECharts`

### 3.2 后端技术栈

- 语言：`Java 17`
- 核心框架：`Spring Boot 3`
- Web 框架：`Spring MVC`
- 安全与密码加密：`Spring Security Crypto` + `PasswordEncoder`
- 数据访问：`Spring Data JPA`
- 复杂统计与导出查询：`JPA 原生 SQL`，必要时逐步补充 `MyBatis`
- 参数校验：`Jakarta Validation`
- 数据库：`MySQL 8`
- 缓存与会话增强：后续可引入 `Redis`
- 异步机制：`Spring Event + @Async`
- 定时任务：`Spring Scheduling`
- 文件存储：当前为 `本地文件存储`，后续可扩展 `MinIO`

### 3.3 部署技术

- 前端部署：`Nginx` 托管静态资源
- 后端部署：`Spring Boot Jar`
- 数据库部署：`MySQL 8`
- 缓存部署：按需增加 `Redis`
- 操作系统建议：`Linux` 优先，Windows 可用于本地开发与课程演示

## 4. 架构最终选择

### 4.1 总体架构

本项目最终采用：

- `前后端分离`
- `B/S 架构`
- `模块化单体`
- `分层架构`
- `DDD-lite`
- `轻量异步`

### 4.2 不采用微服务的原因

当前阶段不采用微服务，原因如下：

- 课程项目数据规模和并发规模有限，没有拆分微服务的必要。
- 核心业务包含较多强事务场景，单体架构更容易保证一致性。
- 微服务会显著增加网关、注册中心、链路追踪、配置中心和联调难度。
- 你们的小组协作更适合基于业务模块分工，而不是基于多个服务拆分。
- `SRS` 已明确首版优先采用 `模块化单体部署方式`。

### 4.3 不采用 Go 的原因

本项目最终不选择 Go 作为后端主语言，原因如下：

- 当前仓库已经具备 Spring Boot 后端基础，切换到 Go 意味着后端重写。
- 该系统属于中后台业务系统，更强调权限、事务、审核、日志和统计，而不是极端高并发。
- Java 生态在 `RBAC`、事务控制、参数校验、审计留痕、复杂查询`等方面更成熟。
- 对课程项目而言，交付确定性比语言切换收益更重要。

## 5. 系统总体架构图

```text
浏览器 / 手机浏览器
        |
   Vue 3 前端
        |
       Nginx
        |
   RESTful API / JSON
        |
 Spring Boot 模块化单体应用
        |
  ├─ 认证与权限模块
  ├─ 用户管理模块
  ├─ 活动管理模块
  ├─ 报名审核模块
  ├─ 签到签退模块
  ├─ 服务记录模块
  ├─ 评价排行模块
  ├─ 内容审核模块
  ├─ 公告通知模块
  ├─ 捐赠支持模块
  ├─ 反馈申诉模块
  ├─ 统计报表模块
  └─ 审计日志模块
        |
  ├─ MySQL
  ├─ 可选 Redis
  ├─ 本地文件存储 / MinIO
  └─ 异步任务与定时任务
```

## 6. 模块划分

建议以如下业务模块作为项目开发和设计的主边界：

- `auth`：注册、登录、登出、会话校验
- `user`：用户资料、角色、账号状态、组织方管理
- `activity`：活动发布、状态流转、活动查询
- `registration`：报名、审核、候补、取消
- `checkin`：签到签退、异常更正
- `service-record`：时长记录、成果证明、统计口径沉淀
- `ranking`：评价、积分、排行、评优
- `content`：内容投稿、审核、发布
- `announcement`：公告发布与展示
- `notification`：站内消息、活动提醒、系统通知
- `donation`：捐赠与支持记录
- `feedback`：反馈、申诉、回执
- `dashboard`：统计报表、可视化指标
- `audit`：审计日志、关键行为追踪

这种划分方式的原因是：

- 与 `SRS` 的功能需求编号可以直接对应。
- 与当前仓库已有包结构基本一致。
- 适合课程小组并行分工，冲突较少。
- 后续增加新能力时，能够局部扩展而不影响全局。

## 7. 前后端选型理由

### 7.1 前端为什么选择 Vue 3，并预留 TypeScript 演进空间

- `Vue 3` 对中后台页面开发效率高，组件化清晰，学习曲线平缓。
- `TypeScript` 有利于接口字段约束、组件参数约束和多人协作，适合作为后续增强方向。
- `Vite` 启动快、构建简单，适合课程项目开发节奏。
- `Element Plus` 和 `ECharts` 适合作为后续 UI 与可视化增强能力。

### 7.2 后端为什么选择 Java + Spring Boot

- 与当前项目仓库连续兼容，重构成本最低。
- 更适合权限、事务、审核、日志、统计这类中后台业务。
- Spring 生态完整，便于快速实现鉴权、校验、事务、异常处理和审计。
- 有利于答辩中解释架构合理性，技术路线成熟，风险可控。

## 8. 数据访问策略

### 8.1 当前策略

当前项目以 `Spring Data JPA` 为主，这与现有代码一致，适合：

- 基础增删改查
- 中等复杂度条件查询
- 业务实体与数据库表的快速映射

### 8.2 后续增强策略

对于以下场景，建议逐步引入更可控的 SQL 查询方式：

- 统计报表
- 审计日志多条件筛选
- 分页导出
- 排行统计
- 复杂聚合查询

增强方式如下：

- 首选：`JPA 原生 SQL`
- 如果复杂查询持续增多：增加 `MyBatis`

这样设计的原因是：

- 可以兼容当前仓库，不需要一次性切换数据访问层。
- 保留简单业务开发效率，同时提升统计类接口的可控性。

## 9. 异步与缓存策略

### 9.1 轻量异步设计

本项目首版采用 `轻量异步`，不直接引入重量级 MQ。

异步适用场景：

- 活动取消通知
- 报名通过/驳回通知
- 活动开始提醒
- 大范围导出任务
- 后续排行重算

技术选择：

- `Spring Event`
- `@Async`
- `Spring Scheduling`

这样设计的原因是：

- 满足 SRS 中“通知不得阻塞主事务”的要求。
- 避免课程项目引入过多运维组件。
- 保持系统结构清晰，便于演示和解释。

### 9.2 Redis 的作用

若系统继续演进，建议引入 `Redis`，主要承担以下职责：

- 登录令牌或会话缓存
- 登录失败计数和临时锁定
- 热点统计缓存
- 短期验证码或一次性令牌
- 异步任务状态缓存

这样设计的原因是：

- 降低数据库压力。
- 提升登录与常用查询性能。
- 为后续外部短信/邮件接口打基础。

## 10. 安全设计原则

系统安全设计采用以下原则：

- 所有受保护接口必须鉴权。
- 所有关键操作必须做角色授权。
- 密码必须加密存储。
- 高风险操作必须写入审计日志。
- 前端与后端通信采用 `HTTPS`。
- 敏感数据导出应保留导出人和时间记录。

实现上建议采用：

- `Spring Security` 负责鉴权和授权
- `PasswordEncoder` 负责密码加密
- `AuthInterceptor` 或过滤链负责身份解析
- `AuditLogService` 负责关键操作留痕

## 11. 最终结论

本项目最终技术路线为：

- 当前前端：`Vue 3 + Vite + Vue Router + CSS`
- 当前后端：`Java 17 + Spring Boot 3 + Spring Security Crypto + Spring Data JPA + MySQL 8`
- 推荐演进：按需引入 `TypeScript`、`Pinia`、`Axios`、`Element Plus`、`ECharts` 和 `Redis`
- 架构：`前后端分离 + 模块化单体 + 分层架构 + DDD-lite + 轻量异步`

一句话概括：

> 本项目采用适合课程场景的模块化单体架构，在保证交付稳定性的前提下，兼顾权限治理、事务一致性、异步通知、统计分析和后续扩展能力。
