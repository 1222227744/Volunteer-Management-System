# 需求实现对应矩阵

本文用于将《软件需求规格说明书_2026-v1.1.docx》中的功能需求、当前代码实现以及《系统设计说明书.md》中的设计分层进行一一对应，便于课程验收、代码走查与后续维护。  
本文默认以“当前仓库已实现”为主，涉及尚未落地但在设计文档中保留的能力时，会明确标注为“课程版简化实现”或“后续扩展”。  
同时，本文参考 `概要设计v1` 与 `志愿者管理系统_可行性分析报告(V2.0)` 的最新口径，对第二次迭代已完成事项和剩余待收口事项做同步标注，避免旧文档与最新源文档脱节。

## 1. 对应原则

- 需求编号以 `SRS 4.2` 中的 `FR-01 ~ FR-08` 为准。
- 设计依据以《系统设计说明书》中的“展示层、接口层、领域层、基础设施层”划分为准。
- 代码对应优先列出“主要入口文件”，避免把一个功能拆成过细的文件清单。
- 若当前实现属于课程版简化实现，会在“说明”列中明确写出。

## 2. 功能需求与代码对应

| SRS 编号 | 需求名称 | 设计说明对应 | 后端主要实现 | 前端主要实现 | 当前实现说明 |
| --- | --- | --- | --- | --- | --- |
| FR-01 | 用户注册登录与权限管理 | 接口层、基础设施层、审计留痕 | `backend/src/main/java/com/volunteer/vms/auth/AuthController.java` `backend/src/main/java/com/volunteer/vms/auth/TokenSessionService.java` `backend/src/main/java/com/volunteer/vms/user/UserController.java` `backend/src/main/java/com/volunteer/vms/audit/AuditLogService.java` | `frontend/src/views/LoginView.vue` `frontend/src/views/RegisterView.vue` `frontend/src/stores/auth.js` `frontend/src/router/index.js` | 已实现注册、登录、登出、当前用户查询、角色控制、角色调整与关键操作审计。账号启停、锁定和支持者独立角色暂未实现。 |
| FR-02 | 志愿活动发布与查询 | 展示层、接口层、领域层中的活动状态规则 | `backend/src/main/java/com/volunteer/vms/activity/ActivityController.java` `backend/src/main/java/com/volunteer/vms/activity/Activity.java` `backend/src/main/java/com/volunteer/vms/activity/ActivityStatus.java` | `frontend/src/views/ActivitiesView.vue` `frontend/src/views/ActivityOpsView.vue` | 已实现活动创建、查询、人数上限控制和基础状态机约束，并补齐活动状态变更通知。活动封面、附件、报名截止时间和复杂报名条件仍待扩展。 |
| FR-03 | 活动报名与审核 | 接口层、领域层中的报名状态流转 | `backend/src/main/java/com/volunteer/vms/activity/ActivityController.java` `backend/src/main/java/com/volunteer/vms/activity/ActivityRegistration.java` `backend/src/main/java/com/volunteer/vms/activity/RegistrationStatus.java` | `frontend/src/views/ActivitiesView.vue` `frontend/src/views/ActivityOpsView.vue` `frontend/src/views/MyRegistrationsView.vue` | 已实现报名申请、组织方审核通过/驳回/取消、报名状态查看，以及活动取消后的报名联动关闭和通知反馈。志愿者主动取消报名仍未单独提供入口。 |
| FR-04 | 签到签退与服务记录 | 接口层、领域层中的签到签退规则、服务记录沉淀 | `backend/src/main/java/com/volunteer/vms/activity/ActivityController.java` `backend/src/main/java/com/volunteer/vms/service/ServiceRecordController.java` `backend/src/main/java/com/volunteer/vms/service/ServiceRecord.java` | `frontend/src/views/ActivityOpsView.vue` `frontend/src/views/ServiceRecordsView.vue` `frontend/src/views/MyRegistrationsView.vue` | 已实现审核通过后签到、签到后签退、签退后登记服务记录，并补齐重复登记防护和组织方活动范围校验。异常更正流程与专门留痕仍待补齐。 |
| FR-05 | 评价与激励管理 | 领域层中的激励规则、统计展示 | `backend/src/main/java/com/volunteer/vms/service/ServiceRecordController.java` `backend/src/main/java/com/volunteer/vms/feedback/ActivityFeedbackController.java` `backend/src/main/java/com/volunteer/vms/user/UserController.java` | `frontend/src/views/RankingView.vue` `frontend/src/views/ServiceRecordsView.vue` `frontend/src/views/MyRegistrationsView.vue` `frontend/src/views/ActivityOpsView.vue` | 已实现“服务时长换积分 + 排行榜”的课程版激励规则，并补齐基于正式服务记录的活动评价提交、查询和概览展示。更复杂的勋章、星级、商城体系暂不纳入本轮。 |
| FR-06 | 内容发布、审核与公告通知 | 展示层、接口层、审计与通知基础设施 | `backend/src/main/java/com/volunteer/vms/content/ContentController.java` `backend/src/main/java/com/volunteer/vms/announcement/AnnouncementController.java` `backend/src/main/java/com/volunteer/vms/notification/NotificationController.java` `backend/src/main/java/com/volunteer/vms/notification/NotificationService.java` | `frontend/src/views/ContentsView.vue` `frontend/src/views/AnnouncementsView.vue` `frontend/src/views/NotificationsView.vue` | 已实现投稿、审核、公告发布和站内通知，`v2` 起广播消息按用户级独立记录落库，已读状态不再共享，并补齐报名审核、活动状态变更、反馈处理等通知场景。 |
| FR-07 | 捐赠与支持管理 | 接口层、数据层 | `backend/src/main/java/com/volunteer/vms/donation/DonationController.java` `backend/src/main/java/com/volunteer/vms/donation/Donation.java` | `frontend/src/views/DonationsView.vue` | 已实现捐赠记录、个人与平台总览，并补齐捐赠提交审计。支付回调、回执下载和支持者独立身份暂不纳入本轮。 |
| FR-08 | 统计分析与互动反馈 | 展示层统计看板、接口层反馈闭环、审计查询 | `backend/src/main/java/com/volunteer/vms/dashboard/DashboardController.java` `backend/src/main/java/com/volunteer/vms/feedback/FeedbackController.java` `backend/src/main/java/com/volunteer/vms/audit/AuditLogController.java` | `frontend/src/views/DashboardView.vue` `frontend/src/views/FeedbacksView.vue` `frontend/src/views/AuditLogsView.vue` | 已实现统计看板、意见反馈、处理回复和真实数据库审计查询/分页/导出，并区分管理员全局统计与组织方活动范围统计。申诉仍并入统一反馈闭环。 |

## 3. 关键实体与 SRS 对应

| 实体/状态 | 作用 | 对应需求 |
| --- | --- | --- |
| `User` / `Role` | 用户账号、角色、积分 | FR-01, FR-05 |
| `Activity` / `ActivityStatus` | 活动主数据与状态流转 | FR-02 |
| `ActivityRegistration` / `RegistrationStatus` | 报名申请、审核、签到、签退、完成 | FR-03, FR-04 |
| `ServiceRecord` | 服务时长和成果留痕 | FR-04, FR-05 |
| `ActivityFeedback` | 活动评价与满意度沉淀 | FR-05 |
| `ContentPost` / `ContentStatus` | 投稿与审核留痕 | FR-06 |
| `Announcement` | 公告主数据 | FR-06 |
| `Notification` | 用户级通知与已读回执 | FR-06, FR-08 |
| `Donation` | 捐赠支持记录 | FR-07 |
| `Feedback` / `FeedbackStatus` | 意见反馈与处理回执 | FR-08 |
| `AuditLog` | 关键操作留痕与审计查询 | FR-01, FR-06, FR-08 |

## 4. 演示数据覆盖情况

启动配置 `vms.demo-data.enabled=true` 时，系统会自动导入以下可展示数据：

- 默认管理员、组织方和多名志愿者账号。
- 已结束、待开展的多场活动。
- 待审核、已通过、已驳回、已完成、已取消的报名数据。
- 已签到签退并沉淀的服务记录。
- 已提交的活动评价数据与评价概览。
- 审核通过、待审核、已驳回的投稿内容。
- 公告、用户级通知和反馈处理回执通知。
- 多笔捐赠记录。
- 已处理与待处理的反馈记录。
- 多条审计日志。

## 5. 当前与最新文档的差异说明

- `FR-02` 中的活动封面、附件、报名截止时间、复杂报名条件尚未细化为独立字段。
- `FR-07` 的支付回调、回执下载为模拟实现，不接入真实支付网关。
- `FR-08` 的申诉场景当前并入统一反馈处理流程，没有单独拆分申诉模块。
- 当前系统仍采用轻量会话令牌，不包含 `JWT/Redis`、短信、邮件、对象存储等扩展能力。
- 数据库迁移治理已从纯运行时补丁演进为“版本化 SQL + 启动兼容判定”的轻量方案，但尚未升级为完整 `Flyway/Liquibase` 体系。

## 6. 当前实现待收口项

以下问题仍应视为后续迭代待办，而不是 `v2` 已完成项：

- 活动附件、封面、报名截止时间和复杂报名资格规则尚未建模。
- 志愿者主动取消报名、异常签到更正、服务记录更正审批尚未形成完整流程。
- 审计覆盖已明显改善，但更多横切操作仍可继续补齐，例如通知手工发布、更多后台治理动作等。
- 自动化测试刚开始建立，覆盖面仍不足，尤其缺少更完整的数据层和前端交互回归测试。
- 数据库迁移目前仍是仓库内轻量实现，后续若继续演进，建议切换为标准迁移框架。

## 7. 第二次迭代开发范围状态

第二次迭代建议以“业务闭环补齐 + 治理能力收口 + 数据一致性增强”为主，不做大规模技术栈重构。当前状态如下：

已完成：

- 活动状态流转约束与活动取消联动
- 广播通知改为用户级已读
- 报名审核、活动取消、反馈处理等通知场景补齐
- 基础活动评价提交与查询
- 审计日志真实筛选、分页和导出
- 组织方数据访问边界收紧
- 数据库约束、索引和迁移方案整理

仍建议继续增强：

- 自动化测试覆盖
- 数据迁移框架标准化
- 更完整的异常更正与附件能力

本轮暂不纳入：

- `Redis/JWT` 登录态升级
- 短信、邮件、真实支付接口
- 通用文件上传和对象存储
- 独立“支持者/捐赠者”账号体系
- 更复杂的勋章、商城、星级激励体系

详细目标见 [第二次迭代范围与目标.md](d:/Documents/WorkSpace/10-Projects/Course/Big-Assignments/softwareEngineer/第二次迭代范围与目标.md)。
