server {
    listen 80;
    server_name localhost;

    # 生产环境建议将 80 端口重定向到 HTTPS；课程本地演示可保留 HTTP。
    # return 301 https://$host$request_uri;

    root /var/www/volunteer-service-frontend;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /api/ {
        proxy_pass http://127.0.0.1:8080;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# HTTPS 部署示例。启用前请替换 server_name、证书路径和前端静态目录。
# server {
#     listen 443 ssl http2;
#     server_name volunteer.example.com;
#
#     ssl_certificate /etc/letsencrypt/live/volunteer.example.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/volunteer.example.com/privkey.pem;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_prefer_server_ciphers on;
#
#     add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
#     add_header X-Content-Type-Options "nosniff" always;
#     add_header X-Frame-Options "SAMEORIGIN" always;
#     add_header Referrer-Policy "strict-origin-when-cross-origin" always;
#
#     root /var/www/volunteer-service-frontend;
#     index index.html;
#
#     location / {
#         try_files $uri $uri/ /index.html;
#     }
#
#     location /api/ {
#         proxy_pass http://127.0.0.1:8080;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto https;
#         proxy_read_timeout 60s;
#     }
# }
