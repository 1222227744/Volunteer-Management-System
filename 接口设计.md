# 接口设计

## 1.1 外部系统接口设计

本系统采用前后端分离架构，前端通过浏览器访问 Vue 应用，后端通过 Spring Boot 暴露 RESTful 接口，并通过 MySQL、文件存储、WebSocket、模拟支付和外部通知任务支撑业务运行。接口设计遵循“边界清晰、统一鉴权、统一响应、异常可追踪、核心业务不被外部通道阻塞”的原则。

系统外部接口不直接耦合具体第三方厂商。课程演示阶段，支付、短信和邮件采用可运行的模拟闭环；后续接入真实服务时，应保持业务层接口不变，仅替换外部适配实现。

| 接口类型 | 接口边界 | 主要协议或入口 | 主要输入 | 主要输出 | 设计说明 |
| --- | --- | --- | --- | --- | --- |
| 前后端业务接口 | 浏览器前端与后端服务 | HTTP/HTTPS，`/api/**` | JWT、JSON 请求体、查询参数 | 统一 JSON 响应 | 前端通过 Axios 统一请求后端，受保护接口必须携带 `Authorization: Bearer <token>`。 |
| 文件传输接口 | 前端与后端文件服务 | `multipart/form-data`，`/api/files/upload` | 文件、文件分类、业务类型、业务编号 | 文件编号、原始文件名、存储信息 | 用于活动附件、内容图片和服务证明材料上传，后端校验文件大小、类型并记录文件审计信息。 |
| 文件下载接口 | 前端与后端文件服务 | HTTP GET，`/api/files/{fileId}` | 文件编号 | 二进制文件流 | 后端根据文件记录读取本地存储文件，前端以 Blob 方式下载或展示。 |
| 站内实时通知接口 | 后端通知模块与前端在线用户 | WebSocket，`/ws/notifications?token=...` | JWT、用户连接信息 | 实时通知事件 | 站内通知以数据库记录为准，WebSocket 仅负责在线推送，推送失败不影响通知落库。 |
| 邮件/短信通知接口 | 后端通知模块与外部通知通道 | 异步任务表，`external_notification_tasks` | 接收人、渠道、标题、内容 | 发送状态、失败原因、重试次数 | 当前实现为邮件/短信模拟通道，保留任务、失败和重试记录；后续可替换为 SMTP 或第三方 SMS API。 |
| 捐赠支付接口 | 后端捐赠模块与支付模拟通道 | REST，`/api/donations/orders` 与 `/simulate-payment` | 捐赠人、金额、留言、回调令牌、模拟支付结果 | 订单状态、正式捐赠记录 | 系统先创建捐赠订单，模拟支付成功后生成正式捐赠记录；失败或取消只更新订单状态。 |
| 数据库接口 | 后端服务与 MySQL | JDBC / Spring Data JPA | 实体对象、查询条件、事务操作 | 持久化数据、查询结果 | 数据库使用 MySQL 8，字符集统一为 `utf8mb4`，核心状态字段使用 Java 枚举与字符串字段映射。 |
| 部署与健康检查接口 | 部署环境与后端服务 | HTTP GET，`/api/health` | 无 | 服务状态、时间戳 | Nginx 和运维脚本可调用健康检查接口判断后端服务是否可用。 |

## 1.2 前后端接口设计

前后端接口采用 RESTful 风格。后端接口统一以 `/api` 为前缀，按业务模块划分资源路径；前端不直接访问数据库，也不绕过后端进行文件或通知操作。除登录、注册和健康检查外，业务接口均需要完成登录认证。涉及管理动作的接口还需要进行角色权限校验。

后端统一响应结构如下：

```json
{
  "code": 200,
  "message": "success",
  "data": {}
}
```

其中 `code` 表示业务处理结果，`message` 表示提示信息，`data` 表示实际业务数据。文件下载接口和统计导出接口返回二进制文件流，不使用上述 JSON 结构。

| 接口分组 | 路径前缀 | 主要功能 | 主要访问权限 |
| --- | --- | --- | --- |
| 认证接口 | `/api/auth` | 用户注册、登录、登出、获取当前用户信息 | 登录、注册公开；其余为登录用户 |
| 用户接口 | `/api/users` | 用户列表、个人资料维护、角色调整、账号状态和实名审核 | 登录用户、管理员 |
| 活动接口 | `/api/activities` | 活动查询、创建、编辑、报名、取消报名、报名审核、签到签退、签到码和考勤更正 | 登录用户、组织方、管理员 |
| 服务记录接口 | `/api/service-records` | 服务记录登记、个人服务档案、指定用户服务记录查询 | 登录用户、组织方、管理员 |
| 内容接口 | `/api/contents` | 内容投稿、我的投稿、公开内容、待审核内容和内容审核 | 登录用户、管理员 |
| 公告接口 | `/api/announcements` | 公告查询和公告发布 | 登录用户、组织方、管理员 |
| 通知接口 | `/api/notifications` | 我的通知、标记已读、外部通知任务查看与重试 | 登录用户、管理员 |
| 捐赠接口 | `/api/donations` | 捐赠订单创建、模拟支付、我的订单、捐赠记录查询 | 登录用户、管理员 |
| 反馈接口 | `/api/feedbacks` | 反馈提交、我的反馈、全部反馈、反馈处理 | 登录用户、管理员 |
| 活动评价接口 | `/api/activity-feedbacks` | 活动评价提交、我的评价、活动评价汇总 | 登录用户、组织方、管理员 |
| 资源对接接口 | `/api/resources` | 公益资源登记、帮扶需求登记、资源匹配和流转状态维护 | 组织方、管理员 |
| 荣誉激励接口 | `/api/honors` | 荣誉候选、荣誉授予、我的荣誉、公开风采展示 | 登录用户、管理员 |
| 文件接口 | `/api/files` | 文件上传、文件下载、文件审计列表 | 登录用户、管理员 |
| 统计接口 | `/api/dashboard` | 统计看板、趋势统计、资源统计、统计 CSV 导出 | 组织方、管理员 |
| 运维接口 | `/api/ops` | 系统配置查看与维护、故障处理记录登记和更新 | 管理员 |
| 审计接口 | `/api/audit-logs` | 审计日志查询、分页和导出 | 管理员 |
| 健康检查接口 | `/api/health` | 服务运行状态检查 | 公开或部署环境访问 |

## 1.3 用户界面接口设计

用户界面接口指前端页面与用户之间的交互方式。系统前端采用 Vue 3 和 Vue Router 实现页面组织，Axios 负责与后端通信。页面入口根据用户角色动态展示，普通志愿者不显示后台治理入口；组织方显示活动执行、资源对接和统计入口；管理员显示用户管理、审计日志、系统运维等后台入口。

界面交互遵循以下规则：

| 设计项 | 设计说明 |
| --- | --- |
| 表单校验 | 登录、注册、活动发布、报名审核、服务记录、捐赠订单、反馈和故障记录等表单必须在提交前进行必填项校验。 |
| 权限展示 | 前端根据当前用户角色隐藏无权限菜单；后端仍进行最终权限校验，防止越权访问。 |
| 状态提示 | 操作成功后显示明确成功提示，操作失败时展示后端返回的失败原因。 |
| 登录失效处理 | Axios 响应拦截器捕获 401 后清理本地登录状态，并引导用户重新登录。 |
| 文件交互 | 文件上传使用浏览器原生文件选择器，上传成功后记录文件编号并绑定业务对象。 |
| 实时通知 | 通知页面建立 WebSocket 连接，收到新通知后刷新站内消息列表。 |
| 数据导出 | 审计日志和统计数据导出为 CSV 文件，由前端接收二进制流并触发浏览器下载。 |

## 1.4 异常与安全接口设计

系统对接口异常进行统一处理。认证失败返回未登录或登录态失效提示；权限不足返回权限不足提示；业务规则不满足时返回明确业务错误；未处理异常返回服务器内部错误，避免将堆栈信息直接暴露给前端。

接口安全设计包括：

| 安全点 | 设计说明 |
| --- | --- |
| 身份认证 | 登录成功后后端签发 JWT，前端后续请求通过 Authorization 请求头携带令牌。 |
| 角色授权 | 管理员、组织方和志愿者按照接口职责进行权限划分，敏感操作必须在后端校验角色。 |
| 业务越权控制 | 组织方只能管理自己发布的活动及相关报名、签到、服务记录和统计数据。 |
| 文件安全 | 上传文件限制类型和大小，保存文件审计记录，避免将用户上传文件直接作为可执行资源处理。 |
| 支付防伪 | 模拟支付接口使用订单回调令牌校验支付操作，防止任意修改订单状态。 |
| 审计留痕 | 用户管理、活动管理、报名审核、签到签退、文件上传、支付模拟、系统配置和故障处理等关键操作写入审计日志。 |
